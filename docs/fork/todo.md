请分析当前项目结构，并基于 Translator 插件开发一个 AI 翻译/处理工具，输出一份完整的实现方案文档（Markdown 格式，存放于 docs/fork 目录下）。

**工具命名**：为插件工具提供英文名称和中文名称，并说明使用场景需覆盖翻译功能。

**功能需求**：
1. AI 大模型配置：支持用户配置 API 地址、模型名称、API Key 等参数。
2. 多组 Prompt 提示词模板：支持用户自定义多组提示词模板，并可为每组模板绑定系统全局快捷键。
3. 快捷键触发处理：当用户按下对应快捷键时，自动读取当前选中的文本或输入框中的文本，根据绑定的提示词模板调用 AI 大模型，返回处理结果。
4. 结果展示：展示 AI 的思考过程与最终结果，思考过程可折叠隐藏，最终结果支持一键快速复制。

**实现方案要求**：
- 结合当前项目的实际代码结构，说明需要修改或新增的文件、模块及关键逻辑。
- 给出具体的实现步骤，包括插件注册、配置界面、快捷键监听、文本获取、AI 调用、结果展示等环节。
- 说明如何将该插件上架到 tools 市场，包括所需配置文件和上架流程。